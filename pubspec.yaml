name: ox_system_flutter_demo
description: A senior-level Flutter demo for OX System.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.0
  dio: ^5.0.0
  get_it: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
